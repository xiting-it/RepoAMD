# RepoAgent dependencies
# Docker image: vllm-dev:rocm7.2.1_navi_ubuntu22.04_py3.10_pytorch_2.9_vllm_0.16.0
# PyTorch 2.9 + vLLM 0.16.0 + ROCm 7.2.1 are PRE-INSTALLED in the image.
# Do NOT reinstall torch or vllm — use --extra-index-url only if upgrading.

# Web framework
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
pyyaml>=6.0
httpx>=0.27

# Indexing / RAG
tree-sitter>=0.22
tree-sitter-python>=0.22
chromadb>=0.5.0

# Embedding + Reranker
sentence-transformers>=3.0
FlagEmbedding>=1.2

# Utilities
rich>=13.7

# ── NOT needed (pre-installed in Docker image) ──
# torch              → PyTorch 2.9 ROCm (in image)
# vllm               → vLLM 0.16.0 (in image)
# ROCm / HIP runtime → ROCm 7.2.1 (in image)
